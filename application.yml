general:
  kafka-topic: "test-topic"

spring:
  kafka:
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: com.example.KafkaTransactionSerializer
